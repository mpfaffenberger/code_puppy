{
  "name": "mist-ts",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile packages/tui/src/index.tsx --outfile dist/mist"
  }
}
