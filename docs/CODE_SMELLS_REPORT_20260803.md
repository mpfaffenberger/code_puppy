# Code Smells Report — `code_puppy` Core

_Generated by a 3-part audit (SMELL_1, SMELL_2, SMELL_3). Research-only — no source files were modified to produce this report._

## Executive Summary

The codebase is functionally solid but shows a consistent, repo-wide pattern: **breadth over depth** — instead of a few catastrophically broken functions, nearly every module in scope is a God Module/God Class that grew past its original single responsibility, and several near-identical patterns are hand-copied instead of abstracted. The five most severe findings, in order of impact: (1) **`code_puppy/command_line/` hosts 10 `*_menu.py` files (~8,000+ lines) that bypass the project's own `register_screen` plugin hook** — a direct, citable violation of AGENTS.md's plugin-vs-core golden rule, especially glaring since 7+ newer features (skills, hooks, spinner, steer-queue, theme, plugin-list, prune) already use that exact hook correctly; (2) `code_puppy/config.py` is a 2944-line God Module with ~153 unrelated free functions covering everything from XDG paths to a self-contained quick-resume subsystem; (3) `code_puppy/tools/common.py`'s `_get_user_approval_impl`/`_get_user_approval_async_impl` are ~160-180 line near-duplicate twins with a correctness risk baked in — a TUI-mode fix was added to only one of the two copies; (4) `code_puppy/model_factory.py`'s `ModelFactory.get_model()` is a ~472-line if/elif type-dispatch chain that violates Open/Closed — every new provider requires editing this one method; and (5) `code_puppy/messaging/rich_renderer.py`'s `RichConsoleRenderer` is a god-class with ~35 `_render_*` methods, the largest a self-admittedly-complex 141-line method. Across all three audit passes, **every single file explicitly named in scope violates AGENTS.md's own 600-line hard cap** (Rule #3), confirming this isn't isolated to one directory but a systemic tech-debt pattern. On the positive side: exception handling, while broad (`except Exception:`), is almost always deliberate and commented; no bare `except:`, no dead imports, and no test-sniffing hacks were found anywhere in scope except one isolated `sys.modules` check in `config.py`.

## Methodology

**Scope:** core modules only — `code_puppy/` root files, `code_puppy/agents/`, `code_puppy/tools/`, `code_puppy/mcp_/`, `code_puppy/command_line/`, and `code_puppy/messaging/`. Explicitly **excludes** `code_puppy/plugins/`, `code_puppy/tui/`, and `tests/`.

**Approach:** a three-part audit run sequentially (SMELL_1: core root + `agents/`; SMELL_2: `tools/` + `mcp_/`; SMELL_3: `command_line/` + `messaging/`, plus final consolidation). Each part used: (a) a line-count scan (`wc -l`) to identify files over the 600-line cap, prioritizing the largest; (b) targeted full/partial reads of the largest files to map class/function structure and identify long-method, god-class, and duplication smells; (c) grep-based pattern search for `except Exception:`/bare `except:`, magic numbers, `TODO`/`FIXME`, and cross-directory imports to check layering; (d) `ruff check --select F401,F841` to confirm no dead imports/unused variables; (e) for SMELL_3 specifically, a direct read of `AGENTS.md`'s golden rule and `code_puppy/callbacks.py`'s hook inventory to check which `command_line/` features should have been plugins.

## Findings

### Core Root & `agents/`

| File | Line Range | Smell Type | Severity | Description | Remediation |
|------|-----------|-----------|----------|--------------|-------------|
| `code_puppy/config.py` | 1–2944 | God Module / SRP violation | H | 2944 lines, ~153 top-level free functions, zero classes; unrelated concerns (XDG paths, subagent limits, retry profiles, config get/set, MCP config parsing, model validation, model-setting get/set, diff/banner colors, command-history normalization, quick-resume subsystem ~26 functions, session autosave, API-key env loading, i18n locale) all living in one file. | Split into `code_puppy/config/` package (`xdg.py`, `model_settings.py`, `quick_resume.py`, `session_autosave.py`, `theme_colors.py`, `command_history.py`), keeping `config/__init__.py` as a re-export surface so existing import sites don't break. |
| `code_puppy/config.py` | 2293–2666 | Cohesive sub-feature buried in a God Module | M | Self-contained ~26-function "quick resume" subsystem living in `config.py` purely because that's where config-ish things go. | Extract verbatim to `code_puppy/config/quick_resume.py`; minimal coupling to the rest of `config.py` beyond `CONFIG_DIR`-style constants. |
| `code_puppy/config.py` | 1265 | Test-detection-in-production-code smell | M | `normalize_command_history()` does `if "pytest" in sys.modules: return` — production logic branches on whether the test runner is importable. | Prefer dependency injection or an explicit `CODE_PUPPY_SKIP_HISTORY_NORMALIZE` flag instead of sniffing `sys.modules`. |
| `code_puppy/cli_runner.py` | 703–1299 | Long Function / SRP violation | H | `interactive_mode()` is 597 lines, mixing initial-command handling, prompt_toolkit fallback, SIGINT wiring, the main REPL loop, and quit/EOF handling. | Split into `_bootstrap_interactive_session()`, `_run_initial_command()`, `_repl_get_next_task()`, `_repl_handle_task()`; `interactive_mode()` becomes orchestration only. |
| `code_puppy/cli_runner.py` | 145–564 | Long Function / SRP violation | H | `main()` is 420 lines: argparse setup, plugin CLI-arg hooks, UI-mode resolution, renderer wiring, banner printing, port allocation, model validation, Ctrl+C handling, API-key loading. | Extract `_build_arg_parser()`, `_bootstrap_renderers(use_textual)`, `_bootstrap_environment(args)`; `main()` becomes a call sequence. |
| `code_puppy/model_factory.py` | 669–1140 | Long Function / Open-Closed violation (type-code smell) | H | `ModelFactory.get_model()` is a ~472-line if/elif chain dispatching on `model_type` string across 12+ provider branches plus a plugin-handler fallback loop reaching 9 indentation levels. Every new built-in provider requires editing this one method. | Replace with a provider-type registry (`Dict[str, Callable]`) mirroring the existing `_CUSTOM_MODEL_PROVIDERS`/`on_register_model_types()` plugin mechanism — collapses to a dict lookup + dispatch. |
| `code_puppy/model_factory.py` | 1117–1140 | Deep nesting | M | Plugin-handler fallback loop reaches 9 indentation levels. | Resolved for free by the registry-based dispatch remediation above, or extract into `_dispatch_plugin_model_type(...)`. |
| `code_puppy/callbacks.py` | 1–1395 | Large-but-repetitive module (not classic duplication) | M | ~55 top-level `on_*` phase functions, mostly 1–5 lines, funneling into 3 shared dispatchers. Repetitive by breadth (1 wrapper per hook), not copy-paste logic duplication. | Low priority; consider splitting into `callbacks/registry.py` + `callbacks/hooks.py` if the file keeps growing. |
| `code_puppy/callbacks.py` | 174–177 | Layering: core reaching into plugins | M | `_get_disabled_plugins()` lazily imports `code_puppy.plugins.config.get_disabled_plugins` from core `callbacks.py`. | Acceptable as a circular-import dodge, but a real core→plugins layering reach. Consider moving `get_disabled_plugins`/`is_plugin_disabled` to a shared low-level module both sides can import. |
| `code_puppy/config.py` | 2517 | Layering: core reaching into plugins | L | `get_quick_resume_location()` imports `detect_git_branch` from `code_puppy.plugins.statusline.payload`. | `detect_git_branch` looks like a generic git helper accidentally under `plugins/statusline/`; relocate to a neutral `code_puppy/git_utils.py`. |
| `code_puppy/model_factory.py` | 591, 1047–1050 | Layering: core reaching into plugins | L | `get_custom_config()`/`get_model()` import from `code_puppy.plugins.claude_code_oauth.utils` and `plugins.gemini_oauth.*` for OAuth credentials. | Consider an `on_resolve_oauth_credential(model_type)` callback hook so plugins register resolvers instead of core importing plugin internals by path. |
| `code_puppy/agents/_runtime.py` | 648–1152 | Long Function / SRP violation | H | `_run_with_mcp_impl()` is ~504 lines with 4 nested function definitions, mixing pause-state reset, prompt sanitization, pydantic-agent building, streaming execution, retry-on-plugin-request, and SIGINT/MCP lifecycle handling. | Extract nested closures to module-level/private-class helpers (`_build_run_context`, `_execute_agent_run`, `_install_run_signal_handlers`); reduce `_run_with_mcp_impl` to orchestration. |
| `code_puppy/agents/_key_listeners.py` | 842–992 | Large parameter list + long function | M | `_posix_read_session(...)` takes 9 positional params and is ~150 lines mixing cbreak-mode closures, the select-loop, EIO retry bookkeeping, and per-char dispatch. | Bundle terminal-session params into a `TerminalSession` dataclass; group the 3 callables into a separate `Callbacks` bundle. |
| `code_puppy/agents/agent_manager.py` | 248–390 | Long function, high cyclomatic complexity | M | `_discover_agents_locked()` is ~142 lines with 3 near-identical nested try/except-around-import-and-register blocks. | Extract shared `_register_agent_classes_from_module(module, message_group_id)` helper, collapsing ~35 duplicated lines. |
| `code_puppy/secret_store.py` | whole file | Clean | — | No smells found; three-tier keyring/chunk/file-fallback logic is well-documented with named constants and narrow, intentional exception guards. | No action needed. |
| `code_puppy/config.py`, `secret_store.py`, `_runtime.py`, `_key_listeners.py`, `agent_manager.py`, `model_factory.py`, `callbacks.py` | ~70 sites | Broad `except Exception:` (no bare `except:` found) | L | Nearly all deliberate, documented fallback patterns (keyring/provider optionality, plugin isolation); a genuine bug could still silently degrade to a fallback path. | Where guarding purely internal logic (not third-party/plugin code), narrow to specific expected exception types. |
| `code_puppy/agents/run_stats.py`, `code_puppy/agents/_builder.py`, `code_puppy/gemini_model.py`, `code_puppy/claude_cache_client.py` | whole files | Over 600-line cap, otherwise clean | L | 690/638/840/883 lines respectively but well-decomposed functions; no acute single-function or duplication smell. | Low priority; split only if they grow further. |

### `tools/` & `mcp_/`

| File | Line Range | Smell Type | Severity | Description | Remediation |
|------|-----------|-----------|----------|--------------|-------------|
| `code_puppy/tools/command_runner.py` | 699–1093 | Long function / SRP violation | H | `run_shell_command_streaming` is ~394 lines with 5 nested closures (`_sink`, `read_stdout`, `read_stderr`, `cleanup_process_and_threads`, `detach_to_background`) — spawns the process, manages reader threads, wires key chords, handles backgrounding, and does cleanup all in one function. Worst single SRP offender in the whole audit. | Extract reader-thread logic into a `_StreamReader` class; move backgrounding + chord-registration into a `ShellStreamSession` context manager. |
| `code_puppy/tools/common.py` | 1234–1615 (`_get_user_approval_impl` / `_get_user_approval_async_impl`) | Duplication (sync/async twin) | H | Two ~160–180 line functions duplicated almost line-for-line (same Panel, diff formatting, lifecycle, 3-choice prompt, `finally` cleanup) except for `await`/async calls — and a TUI-mode early-return fix was added to only the async version. Largest duplication in the whole audit by line count, and a live correctness risk. | Extract shared `_build_approval_panel` + terminal-reset `finally` logic; consider making the sync path a thin `asyncio.run()` wrapper over the async impl so a fix in one twin can't diverge from the other. |
| `code_puppy/tools/common.py` | 1027–1197 (`arrow_select_async` / `arrow_select`) | Duplication (sync/async twin) | M | Near-identical ~85-line functions differing only in `await app.run_async()` vs `app.run()`. | Factor shared `Application`/`KeyBindings`/`Layout` construction into `_build_selector_app(...)`; keep only the run-strategy in the two public wrappers. |
| `code_puppy/tools/file_modifications.py` | 584–746 (`_edit_file`/`_edit_file_async`, `_delete_file`/`_delete_file_async`) | Duplication (sync/async twin) | M | Same 4-way payload dispatch and error handling, differing only by `await`. | Extract the payload-dispatch decision into a shared pure function; thin sync/async wrappers invoke the corresponding sub-op. |
| `code_puppy/mcp_/manager.py` | 514–809 | Duplication (sync/async twin) + primitive obsession | M | `start_server_sync`/`stop_server_sync` re-implement bookkeeping that also happens in the async versions; `_pending_start_tasks` is an ad-hoc dict-of-tasks registry. | Pull the "mark starting" triplet into one helper reused by both entry points; consider a typed `PendingTaskRegistry` wrapper. |
| `code_puppy/mcp_/manager.py` | 108–960 | God class / SRP violation | M | `MCPManager` spans ~850 of 970 lines (88%), owning registry sync, full server lifecycle (sync+async), status querying, and background-task bookkeeping. | Split lifecycle ops into an `MCPServerLifecycleController` collaborator; keep `MCPManager` as a thinner facade. |
| `code_puppy/mcp_/server_registry_catalog.py` | 135–1021 | Data-as-code / magic literal blob | M | `MCP_SERVER_REGISTRY` is an ~886-line hardcoded Python literal; every new pre-configured MCP server requires a source edit + redeploy. | Externalize to bundled JSON/YAML, validated into `MCPServerTemplate` instances on load. |
| `code_puppy/tools/command_runner.py` | 931–1530 (7+ sites) | Magic number (repeated literal) | L | The line-truncation cutoff `256` is a bare literal repeated 7+ times. | Add `MAX_RETAINED_OUTPUT_LINES = 256` as a module constant, reference at all sites. |
| `code_puppy/tools/universal_constructor.py` | 137–192, handler fns | Primitive obsession (mild) | L | 4 action handlers take a loose positional `(context, tool_name, python_code, description)` subset, inviting mismatch as actions grow. | If a 6th action is added, switch to a per-action request dataclass. |
| `code_puppy/tools/file_modifications.py` | 1014–1039 | Defensive-coercion smell (mild) | L | `_try_json_repair` swallows all exceptions to tolerate LLM clients that stringify JSON arrays; malformed-but-truthy strings pass through unchanged. | Acceptable targeted shim; promote to shared `code_puppy/tools/coercion.py` if it grows more call sites. |
| `code_puppy/tools/__init__.py` | 102–190 | Large centralized mapping (borderline) | L | `TOOL_REGISTRY` is a single ~90-entry dict coupling every tool submodule together. | No action now (well under 600 lines); natural first split point if it grows. |
| `code_puppy/mcp_/health_monitor.py` | 63 | Magic number (documented default) | L | `check_interval: int = 30` as a bare default param, not a named module constant. | Low priority; could promote to `DEFAULT_HEALTH_CHECK_INTERVAL_SECONDS`. |
| `code_puppy/tools/agent_tools.py`, `file_operations.py`, `universal_constructor.py`, `mcp_/blocking_startup.py`, `mcp_/agent_bindings.py` | ~35 sites | Broad exception handling (mostly documented) | L | Large majority carry justification comments (e.g. "pragma: no cover - defensive"); a few sites (`common.py:1584,1593`, `universal_constructor.py:320`) swallow silently with no comment/logging. | Add a justification comment or `logger.debug`/`logger.warning` at the uncommented silent sites; leave well-documented sites alone. |
| `code_puppy/tools/browser/browser_manager.py` | 8 sites | Broad exception handling (density flag) | L | Highest density of `except Exception:` of any file in scope (8 in ~550 lines); plausible given inherent browser-automation flakiness, but outside primary SCOPE. | Flag for a future targeted pass on `tools/browser/`; no remediation prescribed here. |

### `command_line/` & `messaging/`

| File | Line Range | Smell Type | Severity | Description | Remediation |
|------|-----------|-----------|----------|--------------|-------------|
| `command_line/*_menu.py` (10 files: `add_model_menu.py`, `model_settings_menu.py`, `autosave_menu.py`, `uc_menu.py`, `diff_menu.py`, `judges_menu.py`, `mcp_binding_menu.py`, `colors_menu.py`, `agent_menu.py`, `set_menu.py`) | whole files (~8,000+ lines) | **Architecture-rule violation** (plugin-vs-core) | H | See dedicated subsection below. | See dedicated subsection below. |
| `add_model_menu.py` (L1167–1405) vs `model_settings_menu.py` (L1110–1317) | `run()` methods | Duplication — missing shared abstraction | H | Both 200+ line `run()` methods independently build a prompt_toolkit `Application` + near-identical keybinding wall + render loop. Confirmed: `@kb.add(` count is 13/14/16/24/14/11/10/8/6/13 across the 10-file menu family (~130 registrations total, zero shared base). 8 files also independently define their own `update_display()`. | Extract a shared `PromptToolkitPickerBase` (common keybindings, `Application` construction, abstract `update_display()`/`_render_*` hook) — highest-value DRY fix in `command_line/`. |
| `messaging/rich_renderer.py` | 130–1364 | God Class / SRP violation | H | `RichConsoleRenderer` owns ~35 `_render_*` methods dispatched from a 108-line if/elif chain. Largest methods: `_render_universal_constructor` (L1070–1211, ~141 lines), `_render_file_listing` (L592–723, ~131 lines), `_do_render_uncoordinated` (L448–556, ~108 lines). `_build_peek_text` is self-flagged `# noqa: C901` by its own authors. | Split into per-message-family renderer modules mixed into `RichConsoleRenderer` via a `_RENDER_DISPATCH` dict built from each module's exports. |
| `command_line/add_model_menu.py` | 502–753 | Long Method | H | `_render_model_details` is ~251 lines — the single largest method found across the whole `command_line/`+`messaging/` scope. | Split into `_format_model_summary_fields`, `_format_model_provider_fields`, and a thin composing `_render_model_details`. |
| `messaging/editor_completion.py` (+ 5 sibling files) | whole files | **Separation-of-concerns violation** — messaging depends on command_line | M | `editor_completion.py` locally imports from 6 different `command_line/*_completion` modules; 5 other `messaging/` files (`editor_paste.py`, `editor_display.py`, `message_queue.py`, `run_ui.py`, `editor_history.py`) also reach into `command_line/` — inverting the expected substrate-vs-consumer layering. | Introduce a `messaging/completion_registry.py` that `command_line/*_completion.py` modules register into, so `messaging/` depends on an abstract interface, not 6 concrete modules by name. |
| `command_line/command_handler.py` | 1–5 | **Architecture-rule violation** (eager side-effect import, no hook) | M | `import code_puppy.command_line.uc_menu # noqa: F401` triggers `@register_command` side effects at import time rather than through any callback hook; `uc_menu.py` (911 lines, full end-user feature) is bolted into core rather than routed through `register_screen`/`custom_command`. | Once `uc_menu.py` is migrated to a plugin, drop this import entirely — plugin discovery replaces the explicit side-effect import. |
| `command_line/` (39 files) & `messaging/` (28 files) | whole directories | 600-line cap violation (Rule #3) | M | All 10 explicitly-named SCOPE files (733–1413 lines) blow past AGENTS.md's own hard cap; confirms the cap violation is systemic, not isolated to this scope (also seen in `config.py`/`cli_runner.py`/`model_factory.py`). | Track as standing tech-debt; prioritize splitting files that also carry an independent SRP/god-class smell first. |
| `command_line/prompt_toolkit_completion.py` | 12 sites | Broad exception handling (density flag) | L | Highest density of `except Exception:` in a single SCOPE file (12 in ~1000 lines); one explicitly commented as intentional (L651). | Spot-check uncommented sites (L345, L385, L429) for a justification comment or debug log. |
| `messaging/renderers.py` | class `InteractiveRenderer` | Small dead-code nugget (not the file-level false alarm it first appeared to be) | L | Verified both `renderers.py` (`SynchronousInteractiveRenderer`, classic path) and `rich_renderer.py` (`RichConsoleRenderer`, message-bus/TUI path) are live and correctly separated — no naming-collision issue. But `InteractiveRenderer`'s own docstring says "this async-based renderer is not currently used." | Confirm zero external references, then remove `InteractiveRenderer`. |
| `messaging/bottom_bar.py` | whole file (750 lines) | Spot-check: no acute additional smell beyond cap violation | L | Cohesive single-responsibility module; no god-method/duplication pattern found beyond the general size violation. | No action beyond the general 600-line remediation. |

## Architecture Rule Violations

AGENTS.md's golden rule: *"nearly all new functionality should be a **plugin** under `code_puppy/plugins/` that hooks into core via `code_puppy/callbacks.py`. Don't edit `code_puppy/command_line/`."* Two distinct, independently-citable violations were found:

1. **Plugin-vs-core golden rule — the `*_menu.py` family.** `code_puppy/callbacks.py` ships a `register_screen` hook (`() -> list[dict]` with `{"command": str, "open": callable(app)}`) built *specifically* so new full-screen menus can be contributed as opt-in plugin screens. It is actively and correctly used by 7+ newer features: `plugins/agent_skills/skills_tui.py`, `plugins/hook_manager/hooks_tui.py`, `plugins/puppy_spinner/spinner_tui.py`, `plugins/steer_queue/queue_tui.py`, `plugins/plugin_list/plugins_tui.py`, `plugins/theme/register_callbacks.py`, `plugins/prune/prune_tui.py`. Yet 10 older `*_menu.py` files (`add_model_menu.py`, `model_settings_menu.py`, `autosave_menu.py`, `uc_menu.py`, `diff_menu.py`, `judges_menu.py`, `mcp_binding_menu.py`, `colors_menu.py`, `agent_menu.py`, `set_menu.py` — ~8,000+ combined lines) live directly in `code_puppy/command_line/` and are imported eagerly for side effects (e.g. `command_handler.py`: `import code_puppy.command_line.uc_menu # noqa: F401`) rather than through the hook system. This is the single strongest, most concrete violation across the entire 3-part audit — the exact extension point exists, is proven in production by other features, and is simply not being used here. **Remediation:** migrate each menu to a `code_puppy/plugins/<menu_name>/` package registering via `register_screen`, one at a time, starting with the smallest/newest (`colors_menu.py`, `mcp_binding_menu.py`) before attempting the largest (`add_model_menu.py`, `model_settings_menu.py`).

2. **600-line hard cap (Rule #3).** AGENTS.md's Rules section states a "600-line hard cap — split into submodules" as an explicit, numbered rule — not a style preference. Every file explicitly named across all three SMELL sub-audits' SCOPE sections violates it: `config.py` (2944), `tools/common.py` (1760), `tools/command_runner.py` (1603), `cli_runner.py` (1538), `callbacks.py` (1395), `messaging/rich_renderer.py` (1364), `command_line/add_model_menu.py` (1413), `command_line/model_settings_menu.py` (1361), `mcp_/server_registry_catalog.py` (1126), `agents/_runtime.py` (1152), `model_factory.py` (1140), and more. This is a second, independently-citable architecture-rule violation, distinct from (but compounding) the plugin/core golden-rule violation above — it confirms the tech debt is systemic across the whole audited surface, not confined to `command_line/`.

3. **Minor core→plugins layering reaches** (lower severity, noted for completeness): `code_puppy/callbacks.py` (core) imports `code_puppy.plugins.config.get_disabled_plugins`; `code_puppy/config.py` imports `code_puppy.plugins.statusline.payload.detect_git_branch`; `code_puppy/model_factory.py` imports OAuth utilities from `plugins.claude_code_oauth`/`plugins.gemini_oauth`. These go the *opposite* direction of the golden rule's main concern (core depending on specific plugins rather than plugins depending on core) and would break silently if those plugins were ever renamed/removed. A `on_resolve_oauth_credential` callback hook, or relocating the shared utilities (`detect_git_branch`, `get_disabled_plugins`) to a neutral core-adjacent module, would close this gap.

4. **Separation-of-concerns within `messaging/` vs `command_line/`.** While not a golden-rule violation in the plugin sense, `messaging/editor_completion.py` and 5 sibling files reaching into `command_line/*_completion` modules inverts the expected "messaging is the lower-level UI substrate, command_line is a consumer" layering — a related, smaller-scope architectural smell worth fixing alongside the `register_screen` migration.

## Appendix: Line-Count Table (all audited files, largest → smallest)

| File | Lines |
|------|------:|
| `code_puppy/config.py` | 2944 |
| `code_puppy/tools/common.py` | 1760 |
| `code_puppy/tools/command_runner.py` | 1603 |
| `code_puppy/cli_runner.py` | 1538 |
| `code_puppy/callbacks.py` | 1395 |
| `code_puppy/messaging/rich_renderer.py` | 1364 |
| `code_puppy/command_line/add_model_menu.py` | 1413 |
| `code_puppy/command_line/model_settings_menu.py` | 1361 |
| `code_puppy/tools/file_operations.py` | 1275 |
| `code_puppy/tools/file_modifications.py` | 1154 |
| `code_puppy/agents/_runtime.py` | 1152 |
| `code_puppy/mcp_/server_registry_catalog.py` | 1126 |
| `code_puppy/model_factory.py` | 1140 |
| `code_puppy/agents/_key_listeners.py` | 1068 |
| `code_puppy/command_line/prompt_toolkit_completion.py` | 1038 |
| `code_puppy/mcp_/manager.py` | 970 |
| `code_puppy/command_line/autosave_menu.py` | 925 |
| `code_puppy/command_line/uc_menu.py` | 911 |
| `code_puppy/tools/universal_constructor.py` | 893 |
| `code_puppy/claude_cache_client.py` | 883 |
| `code_puppy/command_line/diff_menu.py` | 852 |
| `code_puppy/gemini_model.py` | 840 |
| `code_puppy/command_line/judges_menu.py` | 815 |
| `code_puppy/secret_store.py` | 814 |
| `code_puppy/agents/agent_manager.py` | 812 |
| `code_puppy/command_line/core_commands.py` | 733 |
| `code_puppy/messaging/bottom_bar.py` | 750 |
| `code_puppy/command_line/agent_menu.py` | 716 |
| `code_puppy/messaging/bus.py` | 712 |
| `code_puppy/messaging/run_ui.py` | 708 |
| `code_puppy/agents/run_stats.py` | 690 |
| `code_puppy/messaging/line_editor.py` | 693 |
| `code_puppy/tools/subagent_invocation.py` | 678 |
| `code_puppy/command_line/config_commands.py` | 658 |
| `code_puppy/agents/agent_creator_agent.py` | 645 |
| `code_puppy/agents/_builder.py` | 638 |
| `code_puppy/command_line/set_menu_catalog.py` | 649 |
| `code_puppy/command_line/model_picker_completion.py` | 629 |
| `code_puppy/messaging/messages.py` | 611 |
| `code_puppy/mcp_/health_monitor.py` | 589 |
| `code_puppy/messaging/renderers.py` | 558 |
| `code_puppy/command_line/set_menu.py` | 541 |
| `code_puppy/command_line/colors_menu.py` | 538 |
| `code_puppy/mcp_/managed_server.py` | 518 |
| `code_puppy/tools/__init__.py` | 515 |
| `code_puppy/command_line/attachments.py` | 513 |
| `code_puppy/tools/browser/browser_manager.py` | 524 |
| `code_puppy/mcp_/blocking_startup.py` | 479 |
| `code_puppy/command_line/session_commands.py` | 489 |
| `code_puppy/messaging/pause_controller.py` | 500 |
| `code_puppy/command_line/clipboard.py` | 472 |
| `code_puppy/messaging/subagent_console.py` | 468 |
| `code_puppy/messaging/transcript_guard.py` | 466 |
| `code_puppy/command_line/mcp_binding_menu.py` | 400 |
| `code_puppy/messaging/message_queue.py` | 416 |
| `code_puppy/messaging/inline_bar.py` | 401 |
| `code_puppy/command_line/onboarding_wizard.py` | 363 |
| `code_puppy/messaging/bar_painters.py` | 366 |
| `code_puppy/mcp_/retry_manager.py` | 337 |
| `code_puppy/command_line/command_handler.py` | 323 |
| `code_puppy/messaging/editor_completion.py` | 331 |
| `code_puppy/command_line/pin_command_completion.py` | 329 |
| `code_puppy/messaging/bar_rendering.py` | 323 |
| `code_puppy/tools/agent_tools.py` | 324 |
| `code_puppy/mcp_/agent_bindings.py` | 293 |
| `code_puppy/mcp_/circuit_breaker.py` | 290 |
| `code_puppy/command_line/file_path_completion.py` | 225 |
| `code_puppy/tools/fs_access.py` | 229 |
| `code_puppy/messaging/__init__.py` | 305 |
| `code_puppy/messaging/editor_history.py` | 297 |
| `code_puppy/messaging/queue_console.py` | 271 |
| `code_puppy/messaging/commands.py` | 243 |
| `code_puppy/command_line/image_utils.py` | 198 |
| `code_puppy/command_line/set_menu_render.py` | 194 |
| `code_puppy/command_line/onboarding_slides.py` | 188 |
| `code_puppy/messaging/editor_keys.py` | 181 |
| `code_puppy/command_line/command_registry.py` | 186 |
| `code_puppy/tools/io_backends.py` | 186 |
| `code_puppy/command_line/autosave_search.py` | 184 |
| `code_puppy/command_line/mcp_completion.py` | 176 |
| `code_puppy/messaging/editor_paste.py` | 168 |
| `code_puppy/command_line/file_index.py` | 169 |
| `code_puppy/command_line/skills_completion.py` | 160 |
| `code_puppy/messaging/chords.py` | 151 |
| `code_puppy/command_line/config_apply.py` | 152 |
| `code_puppy/command_line/shell_passthrough.py` | 150 |
| `code_puppy/messaging/external_editor.py` | 137 |
| `code_puppy/messaging/editor_display.py` | 132 |
| `code_puppy/command_line/set_menu_values.py` | 126 |
| `code_puppy/command_line/utils.py` | 105 |
| `code_puppy/messaging/prompt_prefix_style.py` | 103 |
| `code_puppy/command_line/load_context_completion.py` | 52 |
| `code_puppy/command_line/set_menu_shims.py` | 49 |
| `code_puppy/command_line/wiggum_state.py` | 45 |
| `code_puppy/command_line/pagination.py` | 43 |
| `code_puppy/messaging/editor_actions.py` | 100 |
| `code_puppy/messaging/markdown_patches.py` | 89 |
| `code_puppy/messaging/run_ui_wiring.py` | 86 |
| `code_puppy/command_line/set_menu_schema.py` | 33 |
| `code_puppy/command_line/set_menu_settings.py` | 26 |
| `code_puppy/command_line/__init__.py` | 1 |

_Note: `command_line/` totals 39 files / 17,130 lines; `messaging/` totals 28 files / 10,930 lines (per SMELL_3 research). Some smaller/less-relevant files in both directories were spot-checked but not individually re-verified with `wc -l` for this appendix — see SMELL_3's Research Notes section 2 for the complete raw inventory if a fully exhaustive re-count is needed._
